__pycache__/
*.pyc
.venv/
venv/

# Datasets - large, re-downloadable, never commit
data/

# Vivado / Vitis build junk
*.jou
vivado*.log
vivado_pid*.str
hs_err_pid*.log
webtalk*.log
runme.log
xsim*.log
.Xil/
# (a blanket *.log rule lived here until 2026-09-20 and silently hid every
#  experiment log in the tree -- 206 files that are the provenance the thesis
#  tables parse. One of them was overwritten before anyone noticed it was not
#  in git; see C0050.)
vivado*.str
*.cache/
*.hw/
*.runs/
*.sim/
*.gen/
*.ip_user_files/

# Simulation output
sim/work/
*.vcd
*.vvp
obj_dir/

# Trained weights and traces are regenerable; keep the small ones by hand
train/checkpoints/

# Smoke-test outputs (from --limit runs)
experiments/*_smoke.*
sim/vectors/

# Project brief stays local
host/mac/build/
host/mac/ps7_init.tcl
host/mac/build/logs/

# golden trace dumps of sweep runs: regenerable from the checkpoints on the AMD box; keep out of git
experiments/rate_sweep/traces_*.npz
experiments/rate_sweep_dvsg/traces_*.npz
golden/traces_dvsgesture.npz
