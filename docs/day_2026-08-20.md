# Day summary — 20 August 2026 (autonomous run, 00:30–10:20)

Everything below is committed and pushed; check ladder **20/20 green** at
the end. No board action, nothing irreversible; the two Claude briefs
remain untracked. Timestamps verified against the clock (a lesson from
last night, taken).

## Year one

- **K axis complete in simulation**: K = 1/2/4/8/16, all bit-identical;
  scatter 76/40/22/13/8 cycles/spike — diminishing returns past K=4
  quantified (experiments/latency_sim/).
- **Event-driven FC layer (D0023)**: designed with logged options, built
  with the sign-off rules applied up front, bit-identical at every K on
  the FIRST run. Every layer of the network now has a verified
  event-driven implementation.
- check_all grew to 20 checks (ED FC, year-two host tests).
- **Still gating M5/M7: the bench DMM.** Nothing else.

## Year two (pivoted to simulation-in-the-loop; brief rewritten)

- **Platform decided (D0024)**: the ES-Parkour recreation on the GPU box
  (MuJoCo, trained teacher 88–100 % at L3) is the simulator AND policy
  source. Its own 11.19M student turned out undertrained (12k iterations).
- **P1 executed end to end in one night**:
  - 58k-parameter FPGA-sized encoder (hardware LIF semantics, no batch
    norm) written into their framework additively; distilled 96k
    iterations (9.2 h).
  - **Behavioural**: equals or beats the 200x-larger reference student —
    step 25 % vs 19 % (L3), 19 % vs 0 % (L6); encoder theoretical energy
    31x lower. Absolute success low for BOTH students: the recipe, not
    encoder capacity, is the limiter (a finding, logged).
  - **Hardware**: final checkpoint quantised (D0008, threshold 16) and
    verified BIT-IDENTICAL on both engines at the 2x64x64 robot geometry
    on real simulated event frames — 1M+ comparisons per engine, ED K=4 at
    3.065 ms/inference at real 6 % activity. Baked r1 weight modules
    regenerated; Vivado parameters for the future 64x64 build recorded.
- **S0 PASS**: sim seed-reproducibility (bit-identical rollout hashes).
- **S1 PASS**: the perception-budget loop (lockstep + wall-clock charge +
  stale-latent on miss + escalation flag) built and proven by injected
  delays — the mechanism that keeps deadline data honest in a simulator.
- Deferred with reasons: DVS-Gesture (shared box at 95 % disk).

## Delivered outside the repo

- Thesis outline v2 (Thesis_Outline_updated_2026-08-20.docx): year-two
  pivot, P1 results, K axis, honest limitations.

## Decisions made while you were away (all in decisions.md, review these)

D0023 (ED FC design), D0024 (year-two platform + geometry + P1 plan),
the all-in-on-FPGA-encoder overnight call, DVS-Gesture deferral.

## Waiting on you (unchanged)

1. The bench DMM (year one's entire critical path).
2. Repo rename, when convenient.
