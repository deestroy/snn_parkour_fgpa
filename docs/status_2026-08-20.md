# Project status — 20 August 2026

One page for the supervisor. Supersedes status_2026-08-19.md. Sections
marked [today] update after the distillation run completes (~10:45).

## Year one (the measured crossover) — state

| milestone | state |
|---|---|
| M0–M4, M6 | **done** — both datapaths timing-clean on the ZedBoard at 100 MHz, bit-identical to golden, DSP=0 by construction |
| latency, measured on silicon | dense 4.409 ms/inference (data-independent); event-driven K=4 1.51 ms (2.9x) at trained rates |
| K axis (simulation) | K=1/2/4/8/16 all bit-identical; diminishing returns past K=4 quantified |
| event-driven coverage | every layer incl. FC now has a bit-identical event-driven implementation (conv layers also at K=4; C1 on silicon) |
| M5 energy | **waits on one physical object: a bench DMM** (0.1–1 mA on 1 A; the fabric delta to resolve is ~2.7 mA at 12 V). Everything else rehearsed: burst windows, manual-entry calculator, protocol doc |
| M7 crossover | latency half known (sim, board-verified to 3 %): ED K=4 never crosses dense in latency; the crossover question is purely ENERGY |

## Year two (simulation-in-the-loop) — started early

The physical robot was dropped (2026-08-20); year two puts the real FPGA
inside a simulated quadruped control loop. Platform: the ES-Parkour
recreation (MuJoCo, trained teacher at 94–100 % success L3). Progress in
the first 24 h of year-two work:

- **P1 (the FPGA-sized network):** a 58k-parameter encoder with the
  hardware's exact neuron semantics is distilling from the trained teacher
  (96k iterations; the paper-sized student had been undertrained at 12k).
  [today: final loss, success rates L3/L6 vs teacher and vs the 11.19M student]
- **The full checkpoint-to-silicon pipeline already works:** real simulated
  event frames -> int8/power-of-two quantisation (threshold 2^4) -> BOTH
  engines bit-identical at the 2x64x64 robot geometry (1M+ comparisons)
  -> 3.06 ms/inference (ED K=4) at the real 6 % activity. Proven on the
  mid-training checkpoint; re-run on the final one [today].
- **S0 (sim reproducibility): PASS** — same seed, bit-identical rollouts.
- **S1 (deadline integrity): PASS** — in a simulator physics waits, so a
  naive loop can never miss a deadline; the perception-budget loop charges
  measured wall-clock time against an explicit budget and delivers STALE
  latents on misses, proven by injected delays. This protects the year-two
  experiment the way the gantry protected the robot.

## What is needed from outside the project

1. **The bench DMM** (unchanged, the whole year-one critical path).
2. Nothing else.
